import crypto  from 'crypto';
export default function() {
  const prefix = 'id';
  const size = 128;
  const bytes = (size/2)-(prefix.length/2);
  return prefix + crypto.randomBytes( bytes ).toString('hex');
}
